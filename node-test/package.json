{
  "name": "mtga-reader-node",
  "version": "0.1.0",
  "description": "Node.js native addon for reading MTGA game data",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "cargo build --release && node scripts/copy-binary.js",
    "build:debug": "cargo build && node scripts/copy-binary.js debug",
    "test": "node test.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">= 16"
  }
}
